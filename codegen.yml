overwrite: true
schema: "http://localhost:3001/graphql"
documents: "./src/app/_graphQL/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
